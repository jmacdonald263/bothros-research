# Python dependencies for reproducing the bothros-research analyses.
#
# The analysis scripts (src/*.py) live in the BOTHROS development repository and are
# available on request (see README -> Reproducing) -- they are not yet packaged here.
# These are the packages they need. Model weights download from the public Hugging
# Face repo (https://huggingface.co/JMacD263/linear-a-linear-b-bothros).
torch>=2.1
torchvision>=0.16
timm>=1.0
ultralytics>=8.1
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
networkx>=3.0
pillow>=10.0
huggingface_hub>=0.20
